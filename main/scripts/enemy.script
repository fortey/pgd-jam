go.property("hp", 10)

local HIT_ID = hash("hit")

function on_message(self, message_id, message)
	if message_id == HIT_ID then
		self.hp = self.hp - 5
		if self.hp<=0 then
			go.delete()
		end
	end
end